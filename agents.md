# Dependencies
- The project uses `uv`.
# Development server
- `uv run rabbit serve` no need to restart the server, it has live reload.
# Linting
- After making changes, run `uv run ruff format`.
# Testing
- The project exclusively uses `pytest`.
- Run using `uv run pytest -vvv`.