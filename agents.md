# Dependencies
- The project uses `uv`.
# Development server
- `uv run rabbithole serve` no need to restart the server, it has live reload.
# Testing
- The project exclusively uses `pytest`.
- Run using `uv run pytest -vvv`.