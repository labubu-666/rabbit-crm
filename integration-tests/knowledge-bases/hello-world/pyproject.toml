[project]
name = "hello-world"
version = "0.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "rabbit-crm",
]

[tool.uv.sources]
rabbit-crm = { workspace = true }
